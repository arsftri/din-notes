<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="mobprog.uts.AdindaRiskaSafitri">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/dinotes"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dinotes_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobprogutsAdindaRiskaSafitri"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- MainActivity which launches the app -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- dinotes_home activity -->
        <activity
            android:name=".dinotes_home"
            android:exported="false" />

        <!-- dinotes_view activity -->
        <activity
            android:name=".dinotes_view"
            android:exported="false" />

    </application>

</manifest>
